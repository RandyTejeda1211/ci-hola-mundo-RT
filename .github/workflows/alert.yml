name: AlertCI

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send alert
        run: |
          curl -X POST https://ntfy.sh/devops-itla \
            -H "Title: Nuevo push a main" \
            -d "Se ha subido código al repositorio 🚀"


